{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run",
            "type": "shell",
            "command": "cmake --build ${workspaceFolder}/build --config Debug ; if ($?) { ${workspaceFolder}/build/bin/DeltaEngine.exe }",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}